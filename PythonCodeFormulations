#  March 24 Lesson Notes

#for i in range(1,5): 
#	print(i, "Hello Python"); 

#num = 0;
#while num < 5:	
#	print( num, "Hello Python"); 
#	print('python likes indention!');

#	if num > 2: break;
#	num += 1;

# function definition and declaration
# def calculate_sum(a,b):
# 	sum = a+b
# 	return sum

# The below statement is called function call
# answer = (calculate_sum(2,3))
# print ("answer is " , answer);


alphabet = "abcdefghijklmnopqrstuvwxyz"

i = 0;
while i<len(alphabet):
	print (alphabet[i]);
	i = i+1;
print(alphabet[1:3])


# todo: palindrome detector
# Python data structures: stacks and queues
